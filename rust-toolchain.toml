# Pinned: reproducing a field failure needs the compiler the unit was built
# with, not whatever stable was that day.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
targets = ["aarch64-unknown-linux-gnu"]
