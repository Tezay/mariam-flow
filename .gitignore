# Internal
.local/
CLAUDE.md
.claude/

# Local capture data (published separately on Hugging Face)
data/

# Build artifacts
target/
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
node_modules/
dashboard/.svelte-kit/

# The dashboard is built in CI and embedded into the daemon binary through
# the `dashboard` feature; its output is never versioned.
dashboard/build/

# Editor
.vscode/

# OS
.DS_Store
